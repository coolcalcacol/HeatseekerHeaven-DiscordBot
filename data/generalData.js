// const { cConsole } = require('../utils/utilityManager.js');

module.exports = {
    client: null,
    debugMode: true,
    logOptions : {
        interactions: true,
        playerData: true,
        gameData: false,
        gameMmrResults: false,
    },
}